.DS_Store
*.log
.env
.playwright-mcp/
.superpowers/

# never commit real trip data — it lives only on the data volume
backups/
state*.json
data/

# local screenshots / scratch
*.jpeg
*.png

# ...except the real PWA icon assets (shipped by the Dockerfile, deployed via git)
!icon-192.png
!icon-512.png
!icon-maskable-512.png
!apple-touch-icon-180.png
!favicon-32.png

# stray Go build output
api/balitrip-expenses-api
*-api
